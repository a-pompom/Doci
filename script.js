import DrawingHandler from './handler/drawingHandler.js'

const pointDrawer = (() => {

    return {

        init() {
            
            const drawingHandler = new DrawingHandler()
        }
    }
    
})()

pointDrawer.init()